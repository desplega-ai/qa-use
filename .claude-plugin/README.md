# `qa-use` Plugin Marketplace for Claude Code

To add the marketplace in your Claude Code:

```bash
/plugin marketplace add desplega-ai/qa-use
```

Then check the [plugins directory](../plugins) to see the available plugins.
